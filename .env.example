# remotectl server 环境变量示例
# 复制为 .env 后修改，docker-compose 会自动加载

# 被控端设备 Token（每台设备一条）
# 格式: REMOTECTL_TOKEN_<device_id>=<secret>
REMOTECTL_TOKEN_office-mac=change-me-strong-secret-1
REMOTECTL_TOKEN_home-win=change-me-strong-secret-2
